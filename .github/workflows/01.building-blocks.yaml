name: 01-Building Blocks
on:
    workflow_dispatch
jobs:
    echo-hello:
        runs-on: ubutntu-latest
        steps:
          - name: Say Hello
            run: "Hello, World"

    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed Step
              run: "i will fail"

            - name: Say goodbye
              run: "Goodbye"
